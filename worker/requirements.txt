# Shared video primitives (FFmpeg util, TTS)
-e ../../../packages/video-pipeline-core

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
python-multipart==0.0.12

# Image (ffmpeg binary via video-pipeline-core)
Pillow==10.4.0

# TTS deps provided by video-pipeline-core (edge-tts, gTTS, mutagen, imageio-ffmpeg)

# LLM (optional — gen script khi user provide GEMINI_API_KEY)
google-generativeai==0.8.3

# Notes:
# - moviepy/whisper REMOVED: dùng ffmpeg trực tiếp + duration đo từ MP3 metadata.
# - SD/torch SKIPPED: design lock = upload-only, không gen ảnh AI.
