import type { SubtitleStyle } from '@/lib/api';

/** Shared Studio defaults (UI + persisted draft fallbacks). */
export const DEFAULT_STUDIO_VOICE = 'en-US-JennyNeural';
export const DEFAULT_STUDIO_SUBTITLE_STYLE: SubtitleStyle = 'off';
